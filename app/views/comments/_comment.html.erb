<%= div_for comment do %>
  <p>
	<span class="content"><%=comment.body %></span><br>
	<span class="timestamp">  
		Posted <%= time_ago_in_words(comment.created_at) %> ago by <%= comment.user.name %>
	</span>
<% end %>
